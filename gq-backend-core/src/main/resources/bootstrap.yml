spring:
  application:
    name: gq-server
  #微服务
  cloud:
    nacos:
      server-addr: ${gq.cloud.nacos.server-addr}:8848   #nacos地址
      username: ${gq.cloud.nacos.username}
      password: ${gq.cloud.nacos.password}